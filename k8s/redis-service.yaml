apiVersion: v1
kind: Service

metadata:
  name: devops-catalog-redis
  namespace: devops-catalog

spec:
  type: ClusterIP

  selector:
    app: devops-catalog-redis

  ports:
    - port: 6379
      targetPort: 6379