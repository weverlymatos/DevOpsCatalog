apiVersion: v1
kind: Service
metadata:
  name: devops-catalog-api
  namespace: devops-catalog

spec:
  type: ClusterIP

  selector:
    app: devops-catalog-api

  ports:
    - port: 80
      targetPort: 8080